# Functional analysis

For each consequential interaction record source component, target component, action, function class (`useful`, `harmful`, `insufficient`, or `excessive`), evidence, consequence, and controllability.

Do not classify a component itself as harmful. Classify an interaction and its observed effect.
